{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "setup": "npm install --prefix client",
    "build": "npm run build --prefix client",
    "heroku-postbuild": "NODE_ENV=false npm run setup && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "child-process": "^1.0.2",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "express": "^4.17.1",
    "firebase-admin": "^9.5.0",
    "fs-extra": "^9.1.0",
    "morgan": "^1.10.0",
    "razorpay": "^2.0.6"
  }
}
